import { Dashboard } from "@/components/dashboard";

export const metadata = {
  title: "Dashboard | UNTRACED",
  description: "Manage your zero-knowledge verification flows",
};

export default function DashboardPage() {
  return <Dashboard />;
}
